version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "8085:8085"
    environment:
      # If you need to change backend IPs dynamically, Vite needs variables prefixed with VITE_.
      # Since we hardcoded the IPs in the JS files based on the IP list, it will work.
      - NODE_ENV=development
