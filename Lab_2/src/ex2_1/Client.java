package ex2_1;

public class Client {
    public static void main(String[] args) {
        Order order = new Order();
        order.executeMove();
        order.executeMove();
        order.executeMove();
        order.executeMove();
        order.executeMove();
        order.executeMove();
    }
}
